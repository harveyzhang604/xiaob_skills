# 💰 Profit Hunter ULTIMATE - 依赖包
# =====================================

# 核心依赖（必须）
requests>=2.31.0
pandas>=2.0.0

# Google Trends 支持（必须，不再是可选）
pytrends>=4.9.0

# 定时任务（必须，用于 6 小时自动运行）
schedule>=1.2.0

# 浏览器自动化（强烈推荐，用于 SERP 分析）
playwright>=1.40.0
# 安装后需要运行: playwright install chromium

# 数据可视化（可选）
# matplotlib>=3.7.0
# seaborn>=0.12.0
